package Front.AST.CSS.Selector;

import Front.AST.ASTNode;

public class SelectorNode extends ASTNode {
    public SelectorNode(String name, int line) {
        super(name, line);
    }

    @Override
    public void printTree(int indent) {
        super.printTree(indent);
    }
}
